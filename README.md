# OrCAD-Weight-Control-System-Storage-Shelf
An OrCAD analog design for an automated shelf monitor using a resistive mass sensor. Sensor resistance is linearly converted to 0V–(Vcc−2V). A dual-comparator window detector checks safe load limits [WL, WH], while LEDs indicate underload, normal load, or overload from a single Vcc supply.
